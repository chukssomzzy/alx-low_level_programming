# include "main.h"

/**
 * mul - makes the product of two interger
 * @a: first interger for multiplication
 * @b: second interger for multiplication
(*
 * Description: Returns a multiple of a and b
 * Return: (a * b)
 */

int mul(int a, int b)
{
	return (a * b);
}
